apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-service
spec:
  selector:
    app: elasticsearch
  ports:
    - port: 9200
      targetPort: 9200
  # ClusterIP (default) — only reachable inside the cluster.
  # Filebeat and Kibana talk to Elasticsearch internally via this service.
