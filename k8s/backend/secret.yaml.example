apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  # Full PostgreSQL connection string. Must match the password in k8s/database/secret.yaml.
  DATABASE_URL: "postgresql://kuberange_user:kuberange_pass@postgres-service:5432/kuberange"
