apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
  # ClusterIP: only reachable within the cluster (nginx proxies to it)
  # no type specified, ClusterIP is default.
